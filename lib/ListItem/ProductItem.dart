class ProductItem {
  String title, images, heroId;
  int id, price, discounted, is_variation;

  ProductItem(
      {this.id,
      this.title,
      this.images,
      this.heroId,
      this.price,
      this.discounted,
      this.is_variation});
}
